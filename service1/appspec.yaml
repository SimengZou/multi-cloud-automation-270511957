version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: <TASK_DEFINITION_ARN> # CodeBuild will replace this
        LoadBalancerInfo:
          ContainerName: "service1"
          ContainerPort: 3000